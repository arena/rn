import headToToeAssessment from './head-to-toe-assessment.yml';

// Add new skill imports here, then add them to the skills array below
const skillsData = {
    skills: [
        headToToeAssessment,
    ]
};

export default skillsData;
